// user model enum
enum ActiveStatus {
  ACTIVE
  BLOCKED
}

enum Role {
  USER
  ADMIN
  AUTHOR
}

enum PostStatus {
  DRAFT
  ARCHIVED
  PUBLISHED
}

enum CommentStatus {
  APPROVED
  REJECT
}

enum SubscriptionStatus {
  ACTIVE
  CANCELED
  EXPIRED
}
